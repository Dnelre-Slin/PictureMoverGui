<UserControl x:Class="PictureMoverGui.Components.DirectorySelectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PictureMoverGui.Components"
             mc:Ignorable="d" 
             d:DesignHeight="90" d:DesignWidth="800">
    <Grid>
        <GroupBox Grid.Row="0" Padding="10 0">
            <GroupBox.Header>
                <WrapPanel>
                    <Label MinWidth="500" FontSize="20" Content="{DynamicResource SourceLabelContent}"/>
                    <Button Padding="10 0" Margin="5 0" FontSize="20" Command="{Binding OpenFolderBrowserDialog}" x:Name="btnChooseSourceDir" Content="{DynamicResource FolderButtonContent}" IsEnabled="{Binding IsNotRunningGatherer}"/>
                    <Button Padding="10 -20" FontSize="20" Command="{Binding CancelGatherer}" x:Name="btnChooseSourceDirCancel" Content="{DynamicResource CancelContent}" Visibility="{Binding CancelVisibility}"/>
                </WrapPanel>
            </GroupBox.Header>
            <Label x:Name="SourceDirLabel" FontSize="20" Background="GhostWhite" Content="{Binding SourcePath}"/>
        </GroupBox>
    </Grid>
</UserControl>
