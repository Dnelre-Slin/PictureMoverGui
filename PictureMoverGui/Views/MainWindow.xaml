<Window x:Class="PictureMoverGui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:properties="clr-namespace:PictureMoverGui.Properties"
        xmlns:local="clr-namespace:PictureMoverGui"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        mc:Ignorable="d"
        Title="{DynamicResource MainWindowTitle}" Height="500" Width="800" MinHeight="500" MinWidth="800" Icon="/Images/PictureSorterIcon.ico">
    <Window.Resources>
        <local:PictureMoverModel x:Key="moverModel"></local:PictureMoverModel>
    </Window.Resources>
    <Grid>
        <StackPanel>
            <TextBlock Text="{Binding Name}"></TextBlock>
            <Button Command="{Binding ChangeName}" Content="{Binding Description}"></Button>
            <TextBox Text="{Binding TypeText, UpdateSourceTrigger=PropertyChanged}"></TextBox>
            <TextBlock Text="{Binding TypeText}"></TextBlock>
            <CheckBox Command="{Binding CheckBoxChange}" IsChecked="{Binding Active}" ></CheckBox>
        </StackPanel>
    </Grid>

</Window>
