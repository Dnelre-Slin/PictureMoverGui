<UserControl x:Class="PictureMoverGui.Views.PhoneInputView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PictureMoverGui.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Margin="50">
            <TextBlock FontSize="20" Text="Elem 1"></TextBlock>
            <TextBlock FontSize="20" Text="Elem 2"></TextBlock>
            <TextBlock FontSize="20" Text="Elem 3"></TextBlock>
            <ComboBox FontSize="20" ItemsSource="{Binding ItemChoices}" SelectedItem="{Binding ChosenString}"></ComboBox>
            <Button Content="Refresh" Command="{Binding RefreshDevices}" FontSize="20"></Button>
            <Button Content="Connect" Command="{Binding ConnectDevice}" FontSize="20"></Button>
        </StackPanel>
    </Grid>
</UserControl>
