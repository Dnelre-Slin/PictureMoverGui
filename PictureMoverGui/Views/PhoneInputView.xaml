<UserControl x:Class="PictureMoverGui.Views.PhoneInputView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:subviews="clr-namespace:PictureMoverGui.SubViews"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.7*"></RowDefinition>
            <RowDefinition Height="0.7*"></RowDefinition>
            <RowDefinition Height="0.7*"></RowDefinition>
            <RowDefinition Height="1*"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <WrapPanel>
                <TextBlock FontSize="20" Foreground="{Binding PhoneConnectedColor}" Text="{Binding PhoneConnected}"></TextBlock>
                <TextBlock FontSize="20" Margin="10 0" Foreground="{Binding PhoneUnlockedColor}" Text="{Binding PhoneUnlocked}"></TextBlock>
                <Button FontSize="20" Width="23" Height="24" Padding="-12" Content="🔄" Command="{Binding RefreshUsbDevices}"></Button>
                <TextBlock FontSize="20" Margin="10 0" Text="{Binding PhoneChosenName}"></TextBlock>
                <ComboBox FontSize="20" MinWidth="100" ItemsSource="{Binding MediaDeviceChoices}" SelectedValue="{Binding PhoneChosenName}" Visibility="{Binding PhonePickerVisibility}"></ComboBox>
                <TextBlock FontSize="20" Margin="10 0 0 0" Text="Sist kjørt: "></TextBlock>
                <TextBlock FontSize="20" Margin="10 0 0 0" Text="{Binding LastRunDateTime}"></TextBlock>
            </WrapPanel>
            <WrapPanel>
                <TextBlock FontSize="20" Margin="10 0" Text="File count : "></TextBlock>
                <TextBlock FontSize="20" Margin="10 0" Text="{Binding InfoFileCount}"></TextBlock>
            </WrapPanel>
            <!--<Button Content="Refresh" Command="{Binding RefreshDevices}" FontSize="20"></Button>
            <Button Content="Connect" Command="{Binding ConnectDevice}" FontSize="20"></Button>-->
        </StackPanel>
        <StackPanel Grid.Row="1">
            <WrapPanel>
                <TextBlock FontSize="20" Foreground="{Binding RemovableDeviceConnectedColor}" Text="{Binding RemovableDeviceConnected}"></TextBlock>
                <Button FontSize="20" Width="23" Height="24" Padding="-12" Content="🔄" Command="{Binding RefreshUsbDevices}"></Button>
                <TextBlock FontSize="20" Margin="10 0" Text="{Binding RemovableDeviceChosenName}"></TextBlock>
                <ComboBox FontSize="20" MinWidth="100" ItemsSource="{Binding RemovableDeviceChoices}" SelectedValue="{Binding RemovableDeviceChosenName}" Visibility="{Binding RemovableDevicePickerVisibility}"></ComboBox>
            </WrapPanel>
            <TextBlock FontSize="20" Margin="10 0 0 0" Text="{Binding LastRunDateTime}"></TextBlock>
        </StackPanel>
        <subviews:DirectorySelectorLiteView Grid.Row="2" DataContext="{Binding DestinationDirectorySelector}"></subviews:DirectorySelectorLiteView>
        <subviews:SorterInterfaceView Grid.Row="3" DataContext="{Binding SorterInterface}"></subviews:SorterInterfaceView>
    </Grid>
</UserControl>
